function Anime() { 
// React components should start with an uppercase letter.
// Lowercase tags are treated as HTML elements (e.g., <div>, <p>),
// while uppercase names are treated as custom React components.
    return(
        <h3>Anime is cool!</h3>
    ) // we can send this to any other component and use it there. This is called component reusability.
}

export default Anime

//file name may be capital or small -> just keep it capital