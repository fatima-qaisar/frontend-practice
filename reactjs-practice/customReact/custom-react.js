// Trying to simulate how React represents a UI element internally.
// (This is a simplified version, not the actual React object.)
const reactElement ={

}

const mainContainer = document.querySelector('#root');
